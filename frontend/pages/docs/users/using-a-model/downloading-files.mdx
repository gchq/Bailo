import DocsWrapper from 'src/docs/DocsWrapper'
import Image from 'next/legacy/image'
import Box from '@mui/material/Box'

import downloadfile1 from 'public/docs/bailo_file_download_filemanagement.png'
import downloadfile2 from 'public/docs/bailo_file_download_releases.png'
import downloadfile3 from 'public/docs/bailo_file_download_release_page.png'

# Downloading Files

Files can be downloaded in three ways. The final two are exclusively for files with associated releases.

## From the File Management tab

Navigate to the **File Management** tab and click the file name to download it.

<Box sx={{ maxWidth: '75%', margin: 'auto' }}>
  <Image alt='clickable file in file management tab' src={downloadfile1} />
</Box>

## From the Releases tab

Open the **Releases** tab and click the download link next to the relevant file.

<Box sx={{ maxWidth: '75%', margin: 'auto' }}>
  <Image alt='view of releases with the clickable file download link' src={downloadfile2} />
</Box>

## From a specific release page

Open an individual release and use the file download link provided.

<Box sx={{ maxWidth: '75%', margin: 'auto' }}>
  <Image alt='view of specific release with the clickable file download link' src={downloadfile3} />
</Box>

export default ({ children }) => <DocsWrapper>{children}</DocsWrapper>
